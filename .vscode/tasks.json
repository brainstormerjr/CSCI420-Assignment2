{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Run",
      "type": "shell",
      "command": "make clean && make && ./hw2 splines/goodRide.sp",
      "options": {
        "cwd": "${workspaceFolder}/hw2"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Custom task to build and run the program in the /hw2 directory."
    }
  ]
}